{
    "name": "graviton",
    "private": true,
    "homepage": "./",
    "proxy": "http://localhost:3001",
    "scripts": {
        "start-dev": "rm -rf build && mkdir build && start bash -c 'cross-env BROWSER=none react-scripts start'",
        "build": "rm -rf build && mkdir build && react-scripts build && rm -rf ./build/static/serverNotFound.html ./build/static/media/logo.svg"
    },
    "dependencies": {
        "@types/axios": "^0.14.0",
        "@types/react": "^16.9.17",
        "@types/react-dom": "^16.9.4",
        "@types/react-select": "^3.0.8",
        "axios": "^0.19.0",
        "react": "^16.12.0",
        "react-dom": "^16.12.0",
        "react-scripts": "^3.3.0",
        "react-select": "^3.0.8",
        "typescript": "^3.7.4"
    },
    "devDependencies": {
        "cross-env": "^6.0.3"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    },
    "license": "ISC"
}
